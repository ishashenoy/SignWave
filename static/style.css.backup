/* FlowState – shared base styles (gold/purple cohesive theme) */

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Inter', sans-serif;
    background: #07030f;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    -webkit-font-smoothing: antialiased;
}

.container {
    text-align: center;
    padding: 2rem;
    max-width: 1200px;
    width: 100%;
}

h1 {
    font-size: 3rem;
    margin-bottom: 0.5rem;
    background: linear-gradient(135deg, #fde68a, #fbbf24, #c4b5fd);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    filter: drop-shadow(0 2px 12px rgba(250, 204, 21, 0.25));
}

.subtitle {
    font-size: 1.2rem;
    margin-bottom: 2rem;
    color: rgba(255, 255, 255, 0.6);
}

.video-container {
    background: rgba(0, 0, 0, 0.3);
    border-radius: 12px;
    padding: 0.75rem;
    border: 1px solid rgba(250, 204, 21, 0.08);
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.4), 0 0 60px rgba(120, 80, 200, 0.1);
    display: inline-block;
    margin: 0 auto;
}

.video-container img {
    display: block;
    max-width: 100%;
    height: auto;
    border-radius: 8px;
}

.info {
    margin-top: 1.5rem;
    font-size: 0.95rem;
    color: rgba(255, 255, 255, 0.5);
}

.info p {
    margin: 0.4rem 0;
}

.hint {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.35);
    font-style: italic;
}

@media (max-width: 768px) {
    h1 {
        font-size: 2rem;
    }
    
    .subtitle {
        font-size: 1rem;
    }
    
    .container {
        padding: 1rem;
    }
}

/* Page-specific styles are self-contained in their templates */
